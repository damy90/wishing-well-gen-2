{
  "name": "wishing-well-gen-2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:facebook": "vite build && node scripts/build-facebook.mjs",
    "build:github-pages": "node scripts/sync-server-assets.mjs && vite build --config vite.github-pages.config.ts && node scripts/build-github-pages.mjs"
  },
  "devDependencies": {
    "bestzip": "^2.2.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
